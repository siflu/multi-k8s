das wäre das encryptete service account file

docker run -it -v "%cd%":/app ruby:2.4 sh
gem install travis
travis login
copy json file into the volumed directory so we can use it in the container
travis encrypt-file service-account.json -r githubUserName/multi-k8s